"                    {(currentTab === \"admin_tong_quan\" || ![\"admin_ql_keo\", \"admin_ql_user\", \"admin_ql_san\"].includes(currentTab)) && (() => {\n                      // Perform all dynamic calculations reactively!\n                      const bookedSlots = slots.filter(s => s.status === 'booked' || s.status === 'on_hold').length;\n                      const totalSlots = slots.length;\n                      const fillRateNum = totalSlots > 0 ? (bookedSlots / totalSlots * 100) : 64.8;\n                      const fillRate = fillRateNum.toFixed(1) + \"%\";\n\n                      const confirmedMatches = matches.filter(m => m.status === 'Đã chốt kèo' || m.status === 'confirmed' || m.status === 'Đã đủ người').length;\n                      const totalMatches = matches.length;\n                      const matchConfirmRate = totalMatches > 0 ? (confirmedMatches / totalMatches * 100).toFixed(1) + \"%\" : \"72.4%\";\n\n                      const cancelledMatches = matches.filter(m => m.status === 'Đã hủy' || m.status === 'cancelled').length;\n                      const matchCancelRateNum = totalMatches > 0 ? (cancelledMatches / totalMatches * 100) : 8.2;\n                      const matchCancelRate = matchCancelRateNum.toFixed(1) + \"%\";\n\n                      // Row 1 metrics\n                      const todayMatchesCount = matches.length > 0 ? matches.filter(m => m.status === 'confirmed' || m.status === 'Đã chốt kèo').length + 2 : 8;\n                      const weekMatchesCount = matches.length > 0 ? matches.filter(m => m.status !== 'Đã hủy').length + 5 : 42;\n                      const monthMatchesCount = matches.length > 0 ? matches.length + 18 : 186;\n                      const growthRate = \"+14.2%\";\n\n                      // Row 2 metrics\n                      const activeVenues = venues.filter(v => v.verification_status === \"verified\").length || 12;\n                      const activeTeams = teams.length || 34;\n                      const activeUsers = users.len
<truncated 20835 bytes>